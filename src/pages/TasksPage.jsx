import React from "react";

// TODO: replace with the real Tasks page — this is a temporary placeholder
// so the app has something to route to at /tasks.
export default function TasksPage() {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="font-heading text-4xl font-medium">Tasks</h1>
      <p className="text-muted-foreground">Placeholder page — send over the real Tasks page content.</p>
    </div>
  );
}
