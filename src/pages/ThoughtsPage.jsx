import React from "react";

// TODO: replace with the real Thoughts page — this is a temporary placeholder
// so the app has something to route to at /thoughts.
export default function ThoughtsPage() {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="font-heading text-4xl font-medium">Thoughts</h1>
      <p className="text-muted-foreground">Placeholder page — send over the real Thoughts page content.</p>
    </div>
  );
}
