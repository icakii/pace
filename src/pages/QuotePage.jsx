import React from "react";

// TODO: replace with the real Daily Quote page — this is a temporary placeholder
// so the app has something to route to at /quote.
export default function QuotePage() {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="font-heading text-4xl font-medium">Daily Quote</h1>
      <p className="text-muted-foreground">Placeholder page — send over the real Daily Quote page content.</p>
    </div>
  );
}
